import React from "react";
import WeatherLocation from "./WeatherLocation/Index";

const LocationList = () => {
  return (
    <div>
      <WeatherLocation city="Bogota, es" />
    </div>
  );
};

export default LocationList;
